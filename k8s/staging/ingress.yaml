apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: elemana-frontend-ingress
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
spec:
  tls:
    - hosts:
        - agent.elemanaoptique.com
      secretName: elemana-front-tls-secret
  rules:
    - host: agent.elemanaoptique.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: elemana-frontend
                port:
                  number: 80
